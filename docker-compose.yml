version: '3.8'

services:
  web:
    build: .
    ports:
      - "5500:5500"
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}
    volumes:
      - ./uploads:/app/uploads
    restart: unless-stopped
